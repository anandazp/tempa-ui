---
// src/components/Navbar.astro
import { siteData } from "../data/site";
import { searchIndex } from "../data/search";
---

<header class="bg-white-ghost border-b border-gray-200 sticky top-0 z-50">
  <div class="w-full mx-auto px-6 py-3">
    <div class="flex lg:justify-between items-center gap-5 lg:gap-10">
      {/* Mobile Toggle Button */}
      <button
        id="menu-btn"
        class="md:hidden w-6 h-6 flex flex-col justify-center items-center gap-1.5 focus-visible:ring-2 focus-visible:ring-black-onyx focus:outline-none"
        aria-label="Menu"
        aria-expanded="false"
        aria-controls="mobile-sidebar"
      >
        <span
          id="menu-icon-line1"
          class="w-5 h-0.5 bg-black-onyx transition-all duration-300" aria-hidden="true"></span>
        <span
          id="menu-icon-line2"
          class="w-5 h-0.5 bg-black-onyx transition-all duration-300" aria-hidden="true"></span>
      </button>
      {/* Logo / Brand */}
      <a href="/" class="text-lg font-medium tracking-tight">
        {siteData.brand}
      </a>

      {/* Desktop Navigation */}
      <nav class="hidden md:flex gap-6 font-medium tracking-tight">
        {
          siteData.navigation.map((link) => (
            <a href={link.href}>{link.label}</a>
          ))
        }
      </nav>

      {/* Desktop Search Trigger */}
      <button
        id="search-trigger"
        class="hidden lg:flex items-center gap-2 ml-auto text-sm text-black-onyx/50 hover:text-black-onyx/90 bg-black-onyx/5 hover:bg-black-onyx/10 rounded-lg px-3 py-1.5 transition-colors cursor-pointer group"
      >
        <svg
          aria-hidden="true"
          xmlns="http://www.w3.org/2000/svg"
          width="16"
          height="16"
          fill="currentColor"
          viewBox="0 0 256 256"
        >
          <path
            d="M229.66,218.34l-50.07-50.06a88.11,88.11,0,1,0-11.31,11.31l50.06,50.07a8,8,0,0,0,11.32-11.32ZM40,112a72,72,0,1,1,72,72A72.08,72.08,0,0,1,40,112Z"
          ></path>
        </svg>
        <span class="text-black-onyx/50 group-hover:text-black-onyx/90"
          >Search documentation</span
        >
        <kbd
          class="hidden lg:inline-flex items-center text-xs text-black-onyx/50 group-hover:text-black-onyx/90 bg-white-ghost border border-black-onyx/10 rounded px-1.5 py-0.5"
        >
          &#8984;K
        </kbd>
      </button>
    </div>
  </div>

  {/* Mobile Sidebar */}
  <div
    id="mobile-sidebar"
    role="dialog"
    aria-modal="true"
    aria-label="Navigation menu"
    class="fixed top-0 left-0 h-full w-64 bg-white shadow-xl z-50 transform -translate-x-full transition-transform duration-300 ease-in-out"
  >
    <div class="p-6">
      {/* Close button inside sidebar (opsional) */}
      <button id="sidebar-close" class="mb-8 text-gray-500 hover:text-gray-800" aria-label="Close menu">
        <svg
          aria-hidden="true"
          xmlns="http://www.w3.org/2000/svg"
          width="20"
          height="20"
          fill="currentColor"
          viewBox="0 0 256 256"
          class="text-black-onyx"
          ><path
            d="M205.66,194.34a8,8,0,0,1-11.32,11.32L128,139.31,61.66,205.66a8,8,0,0,1-11.32-11.32L116.69,128,50.34,61.66A8,8,0,0,1,61.66,50.34L128,116.69l66.34-66.35a8,8,0,0,1,11.32,11.32L139.31,128Z"
          ></path></svg
        >
      </button>

      <nav class="flex flex-col gap-2">
        {
          siteData.sideNavigationDocs.map((link) => (
              <a href={link.href} class="docs-sidebar-menu">
                {link.label}
              </a>
          ))
        }
        {
          siteData.componentsList.map((component) => (
            <a href=`/docs/components${component.href}` class="docs-sidebar-link">{component.label}</a>
          ))
        }
      </nav>
    </div>
  </div>

  {/* Overlay (gelap di belakang sidebar) */}
  <div
    id="overlay"
    role="presentation"
    aria-hidden="true"
    class="fixed inset-0 bg-black/50 z-40 hidden transition-opacity duration-300"
  >
  </div>

  {/* Search Modal */}
  <div
    id="search-modal"
    role="dialog"
    aria-modal="true"
    aria-label="Search documentation"
    class="fixed inset-0 z-50 hidden items-start justify-center pt-[15vh]"
  >
    {/* Backdrop */}
    <div id="search-backdrop" class="absolute inset-0 bg-black-onyx/60" aria-hidden="true"></div>

    {/* Panel */}
    <div
      class="relative w-full max-w-lg bg-white-ghost rounded-xl shadow-2xl overflow-hidden"
    >
      {/* Input */}
      <div class="flex items-center gap-3 px-4 border-b border-black-onyx/10">
        <svg
          aria-hidden="true"
          xmlns="http://www.w3.org/2000/svg"
          width="18"
          height="18"
          fill="currentColor"
          viewBox="0 0 256 256"
          class="shrink-0 text-black-onyx/30"
        >
          <path
            d="M229.66,218.34l-50.07-50.06a88.11,88.11,0,1,0-11.31,11.31l50.06,50.07a8,8,0,0,0,11.32-11.32ZM40,112a72,72,0,1,1,72,72A72.08,72.08,0,0,1,40,112Z"
          ></path>
        </svg>
        <input
          id="search-input"
          type="text"
          placeholder="Search pages..."
          class="flex-1 py-3.5 bg-transparent text-sm text-black-onyx focus-visible:ring-2 focus-visible:ring-black-onyx/30 rounded outline-none placeholder:text-black-onyx/30"
          autocomplete="off"
          autocorrect="off"
          spellcheck="false"
        />
        <button
          id="search-close"
          class="text-black-onyx/30 hover:text-black-onyx transition-colors cursor-pointer"
          aria-label="Close search"
        >
          <svg
            aria-hidden="true"
            xmlns="http://www.w3.org/2000/svg"
            width="18"
            height="18"
            fill="currentColor"
            viewBox="0 0 256 256"
          >
            <path
              d="M205.66,194.34a8,8,0,0,1-11.32,11.32L128,139.31,61.66,205.66a8,8,0,0,1-11.32-11.32L116.69,128,50.34,61.66A8,8,0,0,1,61.66,50.34L128,116.69l66.34-66.35a8,8,0,0,1,11.32,11.32L139.31,128Z"
            ></path>
          </svg>
        </button>
      </div>

      {/* Results */}
      <div id="search-results" class="max-h-80 overflow-y-auto p-2"></div>

      {/* Empty state */}
      <div
        id="search-empty"
        class="hidden px-4 py-8 text-center text-sm text-black-onyx/30"
      >
        No results found
      </div>
    </div>
  </div>
</header>

<script>
  // --- Mobile menu toggle ---
  (function () {
    const menuBtn = document.getElementById("menu-btn");
    const sidebar = document.getElementById("mobile-sidebar");
    const overlay = document.getElementById("overlay");
    const sidebarClose = document.getElementById("sidebar-close");

    let isOpen = false;

    function openMenu() {
      sidebar?.classList.remove("-translate-x-full");
      sidebar?.classList.add("translate-x-0");
      overlay?.classList.remove("hidden");
      document.body.style.overflow = "hidden";
      isOpen = true;
      menuBtn?.setAttribute("aria-expanded", "true");
    }

    function closeMenu() {
      sidebar?.classList.add("-translate-x-full");
      sidebar?.classList.remove("translate-x-0");
      overlay?.classList.add("hidden");
      document.body.style.overflow = "";
      isOpen = false;
      menuBtn?.setAttribute("aria-expanded", "false");
      menuBtn?.focus();
    }

    menuBtn?.addEventListener("click", function () {
      if (isOpen) closeMenu();
      else openMenu();
    });
    if (sidebarClose) sidebarClose.addEventListener("click", closeMenu);
    overlay?.addEventListener("click", closeMenu);

    document.addEventListener("keydown", function (e) {
      if (e.key === "Escape" && isOpen) closeMenu();
    });

    window.addEventListener("resize", function () {
      if (window.innerWidth >= 768 && isOpen) closeMenu();
    });
  })();

  // --- Search ---
  (function () {
    const index = searchIndex;

    const trigger = document.getElementById("search-trigger");
    const modal = document.getElementById("search-modal");
    const backdrop = document.getElementById("search-backdrop");
    const input = document.getElementById("search-input");
    const closeBtn = document.getElementById("search-close");
    const results = document.getElementById("search-results");
    const empty = document.getElementById("search-empty");

    let selectedIndex = -1;
    let currentResults = [];

    function openSearch() {
      modal.classList.remove("hidden");
      modal.classList.add("flex");
      setTimeout(function () {
        input.focus();
      }, 100);
      document.body.style.overflow = "hidden";
      input.value = "";
      selectedIndex = -1;
      renderResults("");
    }

    function closeSearch() {
      modal.classList.add("hidden");
      modal.classList.remove("flex");
      document.body.style.overflow = "";
      input.value = "";
    }

    function highlight(text, query) {
      if (!query) return text;
      let re = new RegExp(
        "(" + query.replace(/[.*+?^${}()|[\]\\]/g, "\\$&") + ")",
        "gi"
      );
      return text.replace(
        re,
        '<mark class="bg-blue-bell/20 text-black-onyx rounded">$1</mark>'
      );
    }

    function renderResults(query) {
      let q = query.trim().toLowerCase();
      if (!q) {
        results.innerHTML = "";
        results.classList.add("hidden");
        empty.classList.add("hidden");
        currentResults = [];
        selectedIndex = -1;
        return;
      }

      let filtered = index.filter(function (entry) {
        return (
          entry.title.toLowerCase().includes(q) ||
          entry.description.toLowerCase().includes(q)
        );
      });

      currentResults = filtered;
      selectedIndex = -1;

      if (filtered.length === 0) {
        results.innerHTML = "";
        results.classList.add("hidden");
        empty.classList.remove("hidden");
        return;
      }

      empty.classList.add("hidden");
      results.classList.remove("hidden");

      let html = "";
      for (let i = 0; i < filtered.length; i++) {
        let entry = filtered[i];
        html +=
          '<a href="' +
          entry.path +
          '" class="flex items-center gap-3 px-3 py-2.5 rounded-lg text-sm transition-colors hover:bg-black-onyx/5 text-black-onyx no-underline" data-index="' +
          i +
          '">' +
          '<div class="flex-1 min-w-0">' +
          '<div class="font-medium truncate">' +
          highlight(entry.title, query) +
          "</div>" +
          '<div class="text-xs text-black-onyx/40 truncate">' +
          highlight(entry.description, query) +
          "</div>" +
          "</div>" +
          '<span class="text-xs text-black-onyx/20 shrink-0">' +
          entry.path +
          "</span>" +
          "</a>";
      }
      results.innerHTML = html;
    }

    trigger.addEventListener("click", openSearch);

    closeBtn.addEventListener("click", closeSearch);
    backdrop.addEventListener("click", closeSearch);

    document.addEventListener("keydown", function (e) {
      if ((e.ctrlKey || e.metaKey) && e.key === "k") {
        e.preventDefault();
        if (modal.classList.contains("hidden")) openSearch();
        else closeSearch();
      }
      if (e.key === "Escape" && !modal.classList.contains("hidden")) {
        closeSearch();
      }
    });

    input.addEventListener("input", function () {
      renderResults(input.value);
    });

    input.addEventListener("keydown", function (e) {
      let items = results.querySelectorAll("a");
      if (e.key === "ArrowDown") {
        e.preventDefault();
        selectedIndex = Math.min(selectedIndex + 1, items.length - 1);
        updateSelection(items);
      } else if (e.key === "ArrowUp") {
        e.preventDefault();
        selectedIndex = Math.max(selectedIndex - 1, 0);
        updateSelection(items);
      } else if (e.key === "Tab") {
        if (e.shiftKey && document.activeElement === input) {
          e.preventDefault();
          closeBtn.focus();
        } else if (!e.shiftKey && document.activeElement === closeBtn) {
          e.preventDefault();
          input.focus();
        }
      } else if (
        e.key === "Enter" &&
        selectedIndex >= 0 &&
        items[selectedIndex]
      ) {
        window.location.href = items[selectedIndex].getAttribute("href");
      }
    });

    closeBtn.addEventListener("keydown", function (e) {
      if (e.key === "Tab" && !e.shiftKey) {
        e.preventDefault();
        input.focus();
      }
    });

    function updateSelection(items) {
      for (let i = 0; i < items.length; i++) {
        if (i === selectedIndex) {
          items[i].classList.add("bg-black-onyx/5");
          items[i].scrollIntoView({ block: "nearest" });
        } else {
          items[i].classList.remove("bg-black-onyx/5");
        }
      }
    }
  })();
</script>

<style is:global>
  /* Animasi tambahan untuk icon */
  .rotate-45 {
    transform: rotate(45deg);
  }

  .-rotate-45 {
    transform: rotate(-45deg);
  }

  .translate-y-2 {
    transform: translateY(4px);
  }

  .-translate-y-2 {
    transform: translateY(-4px);
  }
</style>
